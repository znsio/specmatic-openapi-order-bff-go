backend:
  host: "order-api-mock"   
  port: "8090"        

kafka:
  host: "localhost"   
  port: "9092"       
  topic: "product-queries"

server:
  port: "8080"

management:
  endpoints:
    web:
      exposure:
        include: "*"



