backend:
  host: "localhost"   
  port: "9000"        

kafka:
  host: "localhost"   
  port: "9092"       
  topic: "product-queries"

server:
  port: "8080"

management:
  endpoints:
    web:
      exposure:
        include: "*"



